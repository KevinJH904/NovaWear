<h2>Acciones</h2>

<div class="row">
    <div class="col-1">
        <?= Formbs::link_regresar(); ?>

    </div>
    <div class="col-1">
        <?= Formbs::link_regresar("Cliente",  "clientes/registrar", "🙋"); ?>
    </div>
</div>
<br>

<h2>Clientes</h2>

<table class="table table-striped table-hover myTable">
    <thead>
    <tr>
        <th>id</th>
        <th>Nombre</th>
        <th>Email</th>
        <th>Teléfono</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($clientes as $p):?>
        <tr>
            <td>
                <?= Html::link("clientes/ver/{$p->id}", "Ver", ["title" => "Ir a producto.."]) ?>
            </td>
            <td><?= $p->nombre?></td>
            <td><?= $p->email?></td>
            <td><?= $p->telefono?></td>
        </tr>
    <?php endforeach;?>
    </tbody>
</table>